#!/bin/bash
# we had `rustdoc *.md -o .` here, but that is wrong because rustdoc can
# take only one input file.
git commit -a -m '--'
git push -u origin master
echo reminder that changes take a few minutes to propagate to the web site
